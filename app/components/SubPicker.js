import { View } from "react-native";
import {Picker} from '@react-native-picker/picker';

export default function SubPicker(){
    <View>
        <Picker>
            <Picker.item label="Selecione um item"></Picker.item>
        </Picker>
    </View>
}